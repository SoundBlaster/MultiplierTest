# MultiplierTest

A tiny starter project initialized with a reusable multiplication utility and tests.

## Quick start

1. Ensure you have Python 3.10+ installed.
2. Run the tests:

```bash
python -m unittest
```

## Usage

```python
from multiplier import multiply

result = multiply(6, 7)
print(result)  # 42
```
